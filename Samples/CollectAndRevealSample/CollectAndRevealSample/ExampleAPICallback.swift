/*
 * Copyright (c) 2022 Skyflow
 */

// No longer needed - use Skyflow.CollectCallback / Skyflow.RevealCallback directly
// (see CollectAndRevealViewController.swift), which already deliver typed responses
// (Skyflow.CollectResponse / Skyflow.RevealResponse) without a custom Callback subclass.
